export interface NoIdUserContact {
    id?: string;
    firstName: string;
    lastName: string;
    email: string;
    message: string;
}

export interface UserContact {
    id: string;
}
