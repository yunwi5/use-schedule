export enum PlannerMode {
	WEEKLY = "weekly",
	MONTLY = "monly",
	YEARLY = "yearly",
	TEMPLATE = "template"
}

export const PlannerModeList = [
	PlannerMode.WEEKLY,
	PlannerMode.MONTLY,
	PlannerMode.YEARLY,
	PlannerMode.TEMPLATE
];
